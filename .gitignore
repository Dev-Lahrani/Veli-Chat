node_modules
dist
*.log
.env
.env.local
.DS_Store
coverage
# Belt-and-suspenders: this app writes nothing to disk by design, but if a
# user creates these folders manually they shouldn't get committed.
.anon-p2p/
.anon-messenger/

# Tor binary and shared libraries (user-provided, platform-specific)
debug/

